# config/config.yaml
# Default experimental configuration.  You can override any field by
# setting the environment variable CONFIG to point to a different YAML
# file, e.g.:
#     CONFIG=my_cfg.yml python -m src.main
seed: 42
env_name: CartPole-v1
total_episodes: 500
max_steps: 200
learning_rate: 0.01
gamma: 0.99
hidden_size: 128
eval_episodes: 20